# Node.js Server Example

This is a simple Node.js server example that runs on port 4001.

## Getting Started

### Prerequisites

- Node.js: Make sure you have Node.js installed on your machine. You can download it from [https://nodejs.org/](https://nodejs.org/).

### Installation

1. Clone the repository:

   ```bash
   git clone https://github.com/Priyankasuradkar-21/Blogger.git
   git checkout master
   
2. Setup the project
    npm i 
    npm test 
    nodemon

# Please remember to run the project we need .env File, for security reasons I cannot expose my .env file